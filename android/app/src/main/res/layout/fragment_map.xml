<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <org.maplibre.android.maps.MapView
        android:id="@+id/map_view"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/btn_toggle_3d"
        android:layout_width="56dp"
        android:layout_height="36dp"
        android:layout_marginTop="80dp"
        android:layout_marginEnd="12dp"
        android:backgroundTint="@color/colorSurface"
        android:elevation="4dp"
        android:text="@string/btn_3d"
        android:textColor="@color/colorPrimary"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/btn_compass"
        android:layout_width="56dp"
        android:layout_height="36dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="12dp"
        android:backgroundTint="@color/colorSurface"
        android:elevation="4dp"
        android:text="@string/btn_compass"
        android:textColor="@color/colorOnBackground"
        android:textSize="18sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/btn_toggle_3d" />

    <!-- Sync indicator dot (C9) -->
    <View
        android:id="@+id/sync_indicator"
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:layout_marginTop="88dp"
        android:layout_marginStart="12dp"
        android:background="@drawable/sync_dot"
        android:elevation="5dp"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Altitude HUD -->
    <TextView
        android:id="@+id/tv_altitude_hud"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginBottom="32dp"
        android:background="@color/colorSurface"
        android:elevation="4dp"
        android:fontFamily="monospace"
        android:paddingHorizontal="10dp"
        android:paddingVertical="6dp"
        android:text="--"
        android:textColor="@color/colorPrimary"
        android:textSize="13sp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Atom count badge (C3) -->
    <com.google.android.material.chip.Chip
        android:id="@+id/chip_atom_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginBottom="8dp"
        android:visibility="gone"
        android:textColor="@color/colorOnBackground"
        app:chipBackgroundColor="@color/colorSurface"
        app:layout_constraintBottom_toTopOf="@id/tv_altitude_hud"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Empty state message (C10) -->
    <TextView
        android:id="@+id/tv_empty_state"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/empty_atoms"
        android:textColor="@color/colorPrimary"
        android:textSize="14sp"
        android:alpha="0.7"
        android:visibility="gone"
        android:padding="16dp"
        android:background="@color/colorSurface"
        android:elevation="4dp"
        app:layout_constraintBottom_toTopOf="@id/chip_atom_count"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="16dp"
        android:layout_marginBottom="8dp" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_my_location"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="12dp"
        android:contentDescription="@string/fab_my_location_desc"
        android:elevation="6dp"
        android:src="@android:drawable/ic_menu_mylocation"
        app:backgroundTint="@color/colorSurface"
        app:fabSize="mini"
        app:tint="@color/colorPrimary"
        app:layout_constraintBottom_toTopOf="@id/fab_drop_atom"
        app:layout_constraintEnd_toEndOf="parent" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_drop_atom"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="24dp"
        android:contentDescription="@string/fab_drop_atom_desc"
        android:elevation="6dp"
        android:src="@android:drawable/ic_menu_add"
        app:backgroundTint="@color/colorPrimary"
        app:tint="@color/colorOnPrimary"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Crosshair overlay — fixed center of screen -->
    <ImageView
        android:id="@+id/iv_crosshair"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:elevation="3dp"
        android:importantForAccessibility="no"
        android:src="@drawable/ic_crosshair"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Crosshair address label — shows Punkto address at crosshair -->
    <TextView
        android:id="@+id/tv_crosshair_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:alpha="0.85"
        android:background="@color/colorSurface"
        android:elevation="3dp"
        android:fontFamily="monospace"
        android:paddingHorizontal="8dp"
        android:paddingVertical="3dp"
        android:textColor="@color/colorPrimary"
        android:textSize="10sp"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/iv_crosshair" />

</androidx.constraintlayout.widget.ConstraintLayout>
